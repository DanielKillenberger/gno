1. **SOL7-R1-DOC-01** — `.flow/handoff/fn-112-gno-sh-docs-brief.md:45`: the brief explicitly says filenames are “best-grounded guesses” and must be confirmed, then uses alternatives such as `src/content/docs/web-ui.{md,mdx}`. This fails the explicit requirement to name the exact `gno.sh` pages/files and prevents the handoff from being mechanically applicable.

SOL_VERDICT: NEEDS_WORK
