# Force LF line endings for all text files (fixes Windows CI)
* text=auto eol=lf

# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads

# PDF fixtures are binary even when their object streams are plain ASCII.
*.pdf binary
